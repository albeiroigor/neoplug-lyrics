[project]
name = "neoplug-lyrics"
version = "0.1.0"
description = "..."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "textual>=0.60",]

[tool.hatch.build.targets.wheel]
include = ["core.py", "ui.py"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

